import{_ as s}from"./plugin-vue_export-helper-x3n3nnut.js";import{r,o as d,c,a as e,d as a,b as i,w as o,e as l}from"./app-2l_F50jD.js";const p="/assets/wait-notify-原理-AOI37sbS.png",h="/assets/JMM抽象示意图-WTtOfkuT.jpg",u="/assets/Java运行时数据区-Ktkj3mXl.png",g="/assets/并发容器-kLdohE4c.png",k="/assets/fork_join流程图-tgZi9ihm.png",m="/assets/工作窃取算法运行流程图-TBEn2WHY.png",b="/assets/join流程图-rxUPLvTq.png",f="/assets/ThreadLocal模型-ieZ-1e1Q.png",y="/assets/ThreadLocal概念图2-w1ko8wJp.png",S={},v=l('<h2 id="基本概念" tabindex="-1"><a class="header-anchor" href="#基本概念" aria-hidden="true">#</a> 基本概念</h2><ul><li>并发：多个任务交替执行 (微观串行，宏观并行)</li><li>并行：多个任务同时执行</li></ul><ul><li><p>同步：一旦开始，调用者必须等到方法调用返回后，才能继续后续的行为</p></li><li><p>异步：一旦开始，调用就会立即返回，调用者可以继续后续的操作</p></li><li><p>临界区：存在对共享资源进行多线程读写操作的代码块</p></li><li><p>竞态条件：多个线程在临界区内执行，由于代码的执行序列不同而导致结果无法预测，称为发生了竞态条件 Race Condition</p></li></ul><ul><li>阻塞：一个线程占用了临界区资源，其它线程必须等待，导致线程挂起，就是阻塞</li><li>非阻塞：没有一个线程可以防碍其他线程执行，所有线程都会尝试不断前进</li></ul><p>多线程的活跃性问题：</p><ul><li>死锁：多个线程都占有其它线程需要的资源，且都不释放，从而没有一个线程可以同时拿到所有资源，造成所有线程都进入等待</li><li>饥饿：某一个或多个线程因为种种原因无法获得所需要的资源，导致一直无法执行</li><li>活锁：多个线程在竞争资源时，都主动释放给其它线程使用，导致资源不断在两个线程间跳动，从而没有一个线程可以同时拿到所有资源</li></ul><p><strong>进程 &amp; 线程</strong></p><p>为了提高OS的工作效率，人们先后提出了批处理OS、进程、线程、协程。</p><ul><li>进程：应用程序分配资源的单位，也就是正在运行的程序。各个进程之间互不干扰，保存着程序运行的状态</li><li>线程：一个进程之内可以分为若干个线程，每个线程负责一个单独的子任务，是 CPU 调度的基本单位</li><li>协程：一种运行在线程之上的用户态模型，也称纤程，在线程的基础上通过时分复用的方式运行多个协程</li></ul><p>进程让操作系统的并发性成为了可能，而线程让进程的内部并发成为了可能。<strong>二者的本质区别在于是否单独占用内存地址及其它系统资源：</strong></p><ol><li>进程单独占有一定的内存地址空间，所以进程间存在内存隔离，数据是分开的，数据共享复杂但是同步简单，各个进程之间互不干扰；而线程共享所属进程占有的内存地址空间和资源，数据共享简单，但是同步复杂。</li><li>进程单独占有一定的内存地址空间，一个进程出现问题不会影响其他进程，不影响主程序的稳定性，可靠性高；一个线程崩溃可能影响整个程序的稳定性，可靠性较低。</li><li>进程单独占有一定的内存地址空间，进程的创建和销毁不仅需要保存寄存器和栈信息，还需要资源的分配回收以及页调度，开销较大；线程只需要保存寄存器和栈信息，开销较小。</li></ol><p>总而言之，线程的优势是通信简单、上下文切换开销低。</p><p><strong>上下文切换</strong></p><p>由于某些原因导致 CPU 不再执行当前的线程，转而执行另一个线程的代码，例如：</p><ul><li>线程的 cpu 时间片用完</li><li>垃圾回收</li><li>有更高优先级的线程需要运行</li><li>线程自己调用了 sleep、yield、wait、join、park、synchronized、lock 等方法</li></ul><p>当发送上下文切换时，OS 需要保存当前线程的状态，并恢复另一个线程的状态，JVM 中对应的就是线程私有的程序计数器，以及栈帧等信息，频繁的上下文切换会影响性能。</p><h2 id="thread" tabindex="-1"><a class="header-anchor" href="#thread" aria-hidden="true">#</a> Thread</h2>',17),T=e("p",null,"Java 中创建主要基于 Thread 和 Running，其中直接创建 Thread 方式实际是合并了 Running 对象，通常更推荐使用 Runnable 接口创建线程，因为：",-1),J=e("ul",null,[e("li",null,[a("Java "),e("em",null,"单继承多实现"),a("的特性使得 Runnable 接口使用起来比 Thread 更灵活")]),e("li",null,"Runnable 更符合面向对象，将线程对象单独进行封装。"),e("li",null,"Runnable 降低了线程对象和线程任务的耦合性")],-1),x=e("h2",{id:"synchronized",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#synchronized","aria-hidden":"true"},"#"),a(" Synchronized")],-1),M=l('<h2 id="wait-notify" tabindex="-1"><a class="header-anchor" href="#wait-notify" aria-hidden="true">#</a> wait/notify</h2><p>Object 对象上了提供了几个 API 用于实现等待唤醒机制，相比于简单的 sleep 更加灵活高效。这些方法底层基于 Object 和 Monitor，因此必须配合 Synchronzied 一起使用：</p><ul><li><code>obj.wait()</code> 让拿到 obj 监视器的线程进入 waitSet 等待</li><li><code>obj.wait(long n)</code> 让拿到 obj 监视器的线程到 waitSet 有限等待</li><li><code>obj.notify()</code> 在 obj 关联的 monitor::waitSet 线程集合中挑一个唤醒</li><li><code>obj.notifyAll()</code> 让 obj 关联的 monitor::waitSet 线程集合全部被唤醒</li></ul><p>注意：</p><ol><li>sleep 是 Thread 方法，而 wait 是 Object 的方法</li><li>wait 需要配合 Synchronized 并且会释放对象锁，而 sleep()不会释放锁资源，仅让出CPU，且不能被唤醒</li><li>notify 不会立即释放CPU和锁资源，而是等同步代码执行完毕才释放</li></ol><p><strong>原理：</strong></p><p><img src="'+p+'" alt="" loading="lazy"></p><p>对 obj 上锁的线程（monitor 的 Owner 线程）发现条件不满足，调用 wait 后会释放 obj 的锁资源，让其它线程竞争，然后进入 monitor 的 waitSet 集合中，转为 WAITING 状态。EntryList 里的 Blocked 线程和 waitSet 里的 WAITING 线程都处于阻塞状态，不会占用 CPU 时间片。</p><p>waitSet 中的线程会在新的 Owner 线程调用 notify/notifyAll 时被唤醒，接着进入 EntryList 列表中和 Blocked 线程一起竞争锁资源。</p><h2 id="park-unpark" tabindex="-1"><a class="header-anchor" href="#park-unpark" aria-hidden="true">#</a> park/unpark</h2>',10),L=l('<h1 id="未整理" tabindex="-1"><a class="header-anchor" href="#未整理" aria-hidden="true">#</a> 未整理</h1><p>-------------------- 整理到此 ------------------------------</p><h3 id="异步模型" tabindex="-1"><a class="header-anchor" href="#异步模型" aria-hidden="true">#</a> 异步模型</h3><p><code>Callable 接口创建任务 -&gt; ExecutorService::submit 提交线程池 -&gt; Future::get 获取结果</code></p><p>另外，FutureTask接口简化了异步模型的使用</p><h3 id="线程间通信" tabindex="-1"><a class="header-anchor" href="#线程间通信" aria-hidden="true">#</a> 线程间通信</h3><ul><li><p>锁与同步 Java中的锁都是基于对象的，因此也称对象锁。而同步指线程之间按照一定的顺序执行。基本的加锁使用synchronized关键字。</p></li><li><p>等待/通知 机制 基于Object::wait(), notify(), notifyAll()方法实现</p></li><li><p>信号量 JDK提供Semaphore类，或者通过volatile关键字实现。volatile保证内存的可见性，一个变量在一个线程里改变，其它线程立即可知。</p></li><li><p>管道 基于管道流的通信方式，使用JDK提供的PipedWriter, PipedReader, PipedOutputStream, PipedInputStream</p></li><li><p>其它还有等待其它线程完成的join(), sleep()，本地线程副本变量工具类ThreadLocal等</p></li></ul><p><strong>sleep()和wait()区别</strong></p><ul><li>wait可以指定时间，也可以不指定；而sleep必须指定时间。</li><li>wait释放cpu资源，同时释放锁；sleep释放cpu资源，但是不释放锁，所以易死锁。（关键）</li><li>wait必须放在同步块或同步方法中，而sleep可以在任意位置。</li></ul><h2 id="原理篇" tabindex="-1"><a class="header-anchor" href="#原理篇" aria-hidden="true">#</a> 原理篇</h2><h3 id="内存模型" tabindex="-1"><a class="header-anchor" href="#内存模型" aria-hidden="true">#</a> 内存模型</h3><h4 id="并发模型" tabindex="-1"><a class="header-anchor" href="#并发模型" aria-hidden="true">#</a> 并发模型</h4><ol><li>线程间如何通信？即：线程之间以何种机制来交换信息</li><li>线程间如何同步？即：线程以何种机制来控制不同线程间操作发生的相对顺序</li></ol><p>解决的两种并发模型：</p><ol><li>消息传递并发模型：线程间必须通过发送消息进行显式通信，线程间没有公共状态</li><li>共享内存并发模型：线程间共享程序的公共状态，通过读写内存的公共状态进行隐式通信</li></ol><p>Java 使用的是 <strong>共享内存并发模型</strong></p><h4 id="jmm" tabindex="-1"><a class="header-anchor" href="#jmm" aria-hidden="true">#</a> JMM</h4><p>Java 线程之间的通信由 Java 内存模型控制，JMM 决定一个线程对共享变量的写入何时对另一个线程可见，是一个抽象模型。</p><p><img src="'+h+'" alt="JMM抽象示意图" loading="lazy"></p><ul><li>Java 线程之间的通信由 JMM 控制，JMM 定义了线程和主内存之间的抽象关系</li><li>线程间通信必须经过主内存</li><li>线程对共享变量的所有操作都必须在自己的本地内存中进行，不能直接操作主内存</li><li>JMM 通过控制主内存与每个线程本地内存之间的交互，来保证内存可见性</li></ul><h4 id="jmm-和-运行时内存" tabindex="-1"><a class="header-anchor" href="#jmm-和-运行时内存" aria-hidden="true">#</a> JMM 和 运行时内存</h4><p><strong>区别</strong></p><p>两者是不同的概念层次。JMM 是抽象的，用来描述一组规则，通过这个规则来控制各个变量的访问方式，围绕原子性、有序性、可见性展开。而 Java 运行时内存的划分是具体的，是 JVM 运行 Java 程序时必要的内存划分。</p><p><img src="'+u+`" alt="Java运行时数据区" loading="lazy"></p><p><strong>联系</strong></p><p>都存在私有数据区域和共享数据区域。一般来说，JMM 中的主内存属于共享数据区域，包含了堆和方法区；同样，JMM 中的本地内存属于私有数据区域，包含了程序计数器、本地方法栈、虚拟机栈。</p><h3 id="重排序" tabindex="-1"><a class="header-anchor" href="#重排序" aria-hidden="true">#</a> 重排序</h3><p>由于流水线技术的产生和中断对流水线的影响，人们提出了指令重排，可以大幅提高 CPU 处理性能。具体分三种：</p><ul><li><p>编译器优化重排 编译器在不改变单线程程序语义的前提下，可以重新安排语句的执行顺序。</p></li><li><p>指令并行重排 现代处理器采用了指令级并行技术来将多条指令重叠执行。如果不存在数据依赖性(即后一个执行的语句无需依赖前面执行的语句的结果)，处理器可以改变语句对应的机器指令的执行顺序。</p></li><li><p>内存系统重排 由于处理器使用缓存和读写缓存冲区，这使得加载(load)和存储(store)操作看上去可能是在乱序执行，因为三级缓存的存在，导致内存与缓存的数据同步存在时间差。</p></li></ul><p>指令重排可以保证串行语义一致，但是没有义务保证多线程间的语义也一致。因此多线程下指令重排可能导致内存可见性问题，需要一些规则来限制。例如 JMM 会禁止特定类型的编译器重排序，以及通过内存屏障来禁止特定类型的处理器重排序，保证不同平台上一致的内存可见性。</p><h4 id="顺序一致性模型" tabindex="-1"><a class="header-anchor" href="#顺序一致性模型" aria-hidden="true">#</a> 顺序一致性模型</h4><p>当程序未正确同步时，可能存在数据竞争，导致运行结果不确定。顺序一致性内存模型是一个理想化的理论参考模型，可以保证内存可见性。有两大特性：</p><ol><li>一个线程中的所有操作必须按照程序的顺序（即Java代码的顺序）来执行。</li><li>不管程序是否同步，所有线程都只能看到一个单一的操作执行顺序。即在顺序一致性模型中，每个操作必须是原子性的，且立刻对所有线程可见。</li></ol><p>JMM没有保证未同步程序的执行结果与该程序在顺序一致性中执行结果一致。因为如果要保证执行结果一致，那么JMM需要禁止大量的优化，对程序的执行性能会产生很大的影响。</p><p>未同步程序在JMM和内存一致性模型下执行差异：</p><ol><li>顺序一致性保证单线程内的操作会按程序的顺序执行；JMM不保证单线程内的操作会按程序的顺序执行。（因为重排序，但是JMM保证单线程下的重排序不影响执行结果）</li><li>顺序一致性模型保证所有线程只能看到一致的操作执行顺序，而JMM不保证所有线程能看到一致的操作执行顺序。（因为JMM不保证所有操作立即可见）</li><li>顺序一致性模型保证对所有的内存读写操作都具有原子性，而JMM不保证对64位的long型和double型变量的写操作具有原子性。</li></ol><h4 id="happens-before" tabindex="-1"><a class="header-anchor" href="#happens-before" aria-hidden="true">#</a> Happens-Before</h4><p>JMM 认为，只要不改变程序的执行结果（单线程程序和正确同步了的多线程程序），编译器和处理器怎么优化都行。JMM 使用 <code>happens-before</code> 规则保证内存可见性，定义如下：</p><ol><li>如果一个操作 happens-before 另一个操作，那么第一个操作的执行结果将对第二个操作可见，而且第一个操作的执行顺序排在第二个操作之前。</li><li>两个操作之间存在 happens-before 关系，并不意味着 Java 平台的具体实现必须要按照 happens-before 关系指定的顺序来执行。如果重排序之后的执行结果，与按 happens-before 关系来执行的结果一致，那么 JMM 也允许这样的重排序。</li></ol><p>Java中具体的规则：</p><ul><li>程序顺序规则：一个线程中的每一个操作，happens-before 于该线程中的任意后续操作。</li><li>监视器锁规则：对一个锁的解锁，happens-before 于随后对这个锁的加锁。</li><li>volatile 变量规则：对一个 volatile 域的写，happens-before 于任意后续对这个 volatile 域的读。</li><li>传递性：如果 A happens-before B，且 B happens-before C，那么 A happens-before C。</li><li>start 规则：如果线程 A 执行操作 ThreadB.start() 启动线程 B，那么 A 线程的 ThreadB.start() 操作 happens-before 于线程B中的任意操作</li><li>join 规则：如果线程 A 执行操作 ThreadB.join() 并成功返回，那么线程 B 中的任意操作 happens-before 于线程 A 从 ThreadB.join() 操作成功返回</li></ul><p>程序员面向的是一条条规则，每条规则对应若干种 编译器/处理器重排序 规则，JMM 通过禁止特定类型的重排序来实现相应的 happens-before 规则。</p><h3 id="volatile" tabindex="-1"><a class="header-anchor" href="#volatile" aria-hidden="true">#</a> Volatile</h3><p>Java 语言规定，如果一个字段被声明成 volatile，Java 线程内存模型确保所有线程看到这个变量的值都是一致的。</p><h4 id="内存语义" tabindex="-1"><a class="header-anchor" href="#内存语义" aria-hidden="true">#</a> 内存语义</h4><ol><li>保证变量的内存可见性</li></ol><ul><li>一个线程对 volatile 变量进行写操作时，JMM 会立即把该线程对应的本地内存中共享变量的值刷新到主内存</li><li>一个线程对 volatile 变量进行读操作时，JMM 会把立即该线程对应的本地内存置为无效，从主内存中读取共享变量的值（基于 MESI 缓存一致性协议）</li></ul><ol start="2"><li>禁止指令重排序：通过内存屏障，阻止屏障两侧的指令重排序。例如单例模式的双重检查实现方式就是为了解决重排序可能产生的错误</li></ol><h4 id="volatile用途" tabindex="-1"><a class="header-anchor" href="#volatile用途" aria-hidden="true">#</a> Volatile用途</h4><p>在保证内存可见性上，volatile 有着与锁相同的内存语义，所以可以作为一个轻量级的锁来使用。但由于 volatile 仅仅保证对单个 volatile 变量的读/写具有原子性，而锁可以保证整个临界区代码的执行具有原子性。所以在功能上锁比 volatile 更强大，而性能上 volatile 更有优势。</p><h3 id="cas" tabindex="-1"><a class="header-anchor" href="#cas" aria-hidden="true">#</a> CAS</h3><p>Synchronized 是一种悲观锁，认为每次访问共享资源时都会发生冲突，所以必须对每次数据操作加上锁，以保证临界区的程序同一时间只能有一个线程在执行。而乐观锁总是假设对共享资源的访问没有冲突，线程可以不停地执行，无需加锁也无需等待，真正发生冲突时再处理。乐观锁通常使用 CAS 保证线程执行的安全性。</p><p>比较并交换 CAS 是一个原子操作，基于 CPU 提供的 CMPXCHG 原子指令实现。其中，有三个值：</p><ul><li>V：要更新的变量 (var)</li><li>E：预期值 (expected)</li><li>N：新值 (new)</li></ul><p>执行时，先判断 V 是否等于 E，如果相等，将 V 的值设置为 N；如果不等，说明已经有其它线程更新了 V，则当前线程放弃更新，什么都不做。</p><p><strong>存在问题</strong></p><ul><li>ABA问题，可以使用时间戳/版本号解决,如 JDK 提供的 AtomicStampedReference</li><li>自旋循环时间过长，可以结合处理器的 pause 指令</li><li>只能保证一个共享变量的原子操作，可以使用 AtomicReference 或 锁机制</li></ul><p>Java 实现 CAS 原理的底层基于 Unsafe 类。基于 Unsafe，JDK 提供了一些用于原子操作的类，在 JUC.atomic 包下，例如 AtomicInteger 等。</p><h3 id="aqs" tabindex="-1"><a class="header-anchor" href="#aqs" aria-hidden="true">#</a> AQS</h3><p>AbstractQueuedSynchronizer抽象队列同步器AQS是一个用来构建锁和同步器的框架，使用AQS能简单且高效地构造出应用广泛的同步器，如ReentrantLock，Semaphore，ReentrantReadWriteLock，SynchronousQueue，FutureTask等等皆是基于AQS的。</p><p>AQS内部使用volatile修饰的state作为资源的标识，维护封装线程和状态的Node结点构成的双向队列，以及Condition条件上的等待线程单向队列。</p><p>另外，还有一个AbstractQueuedLongSynchronizer长整型的队列同步器，两者都继承自AbstractOwnableSynchronizer，用于表示锁与持有者之间的独占关系。</p><h2 id="jdk工具篇" tabindex="-1"><a class="header-anchor" href="#jdk工具篇" aria-hidden="true">#</a> JDK工具篇</h2><h3 id="线程池" tabindex="-1"><a class="header-anchor" href="#线程池" aria-hidden="true">#</a> 线程池</h3><p>使用线程池主要有以下三个原因：</p><ol><li>创建/销毁线程需要消耗系统资源，线程池可以复用已创建的线程。</li><li>控制并发的数量。并发数量过多，可能会导致资源消耗过多，从而造成服务器崩溃。（主要原因）</li><li>可以对线程做统一管理。</li></ol><p><strong>JDK线程池接口/类</strong> 顶层接口Executor，实现类ThreadPoolExecutor</p><div class="language-java line-numbers-mode" data-ext="java"><pre class="language-java"><code><span class="token class-name">ThreadPoolExecutor</span><span class="token punctuation">(</span><span class="token keyword">int</span> corePoolSize<span class="token punctuation">,</span>      <span class="token comment">// 指定常驻线程数量</span>
                   <span class="token keyword">int</span> maximumPoolSize<span class="token punctuation">,</span>   <span class="token comment">// 指定允许的最大线程数量</span>
                   <span class="token keyword">long</span> keepAliveTime<span class="token punctuation">,</span>    <span class="token comment">// 当线程数量超过corePoolSize后，多余的空闲线程的存活时间</span>
                   <span class="token class-name">TimeUnit</span> unit<span class="token punctuation">,</span>         <span class="token comment">// keepAliveTime单位</span>
                   <span class="token class-name">BlockingQueue</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">Runnable</span><span class="token punctuation">&gt;</span></span> workQueue<span class="token punctuation">,</span> <span class="token comment">// 任务队列，保存被提交但尚未被执行的任务</span>
                   <span class="token class-name">ThreadFactory</span> threadFactory<span class="token punctuation">,</span>       <span class="token comment">// 线程工厂，用于创建线程</span>
                   <span class="token class-name">RejectedExecutionHandler</span> handler   <span class="token comment">// 任务过多时的拒绝策略</span>
                   <span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>任务队列</strong> 指被提交但未执行的任务队列，一个BlockingQueue接口的对象，仅用于存放Runnable对象。常用的队列实现有：</p><ul><li><code>SynchronousQueue</code>: 直接提交的队列，没有容量，来一个任务执行一个，没有多余线程则执行拒绝策略</li><li><code>ArrayBlockingQueue</code>: 有界任务队列，构造时指定容量</li><li><code>LinkedBlockingQueue</code>: 无界任务队列，任务繁忙时会一直创建线程执行，直至资源耗尽</li><li><code>PriorityBlockingQueue</code>: 带有执行优先级的无界队列</li></ul><p><strong>拒绝策略 - RejectedExecutionHandler</strong> 指定当任务数量超过系统实际承载能力时的策略，通常是线程池中的线程已经用完，达到了最大线程数，排队队列也已满的情况。ThreadPoolExecutor提供了以下策略：</p><ul><li><code>AbortPolicy</code>: 默认策略，丢弃并抛出RejectedExecutionException异常</li><li><code>CallersRunsPolicy</code>: 绕过线程池，由主线程直接调用任务的run()方法执行</li><li><code>DiscardOldestPolicy</code>: 抛弃队列中等待最久的任务，然后尝试再次提交当前任务</li><li><code>DiscardPolicy</code>: 丢弃且不抛异常</li></ul><p><strong>ThreadPoolExecutor状态</strong> 线程池本身有一个调度线程，因此有自己的状态。包括：Running, Shutdown, Stop, Tidying, Terminated</p><p><strong>执行流程</strong></p>`,74),A=l('<p><strong>线程复用</strong> 根据使用的阻塞队列、线程数限制等，ThreadPoolExecutor::addWorder, runWorker, getTask负责线程的复用、任务调度、执行等</p><p><strong>常见线程池的实现</strong></p><ul><li><code>newSingleThreadExecutor</code>: 只有一个核心线程的线程池</li><li><code>newFixedThreadPool</code>: 固定线程数量的线程池，只有核心线程。新任务提交时，若有空闲线程立即执行，否则暂存在任务队列中。</li><li><code>newCachedThreadPool</code>: 可调整线程数量的线程池，只有非核心线程。新任务提交时，优先使用可复用线程，否则创建新线程处理任务，最多MAX_VALUE个。所有线程完成后返回线程池，空闲线程有存活时间，超时回收。</li><li><code>newSingleThreadScheduledExecutor</code>: 返回一个ScheduledExecutorService对象，线程数量为1。用于实现延时、周期任务。如果中途任务出现异常，后续执行都会被中断。 <ul><li>schedule(Runnable, delay, TimeUnit) 延迟执行任务</li><li>scheduleAtFixedRate(Runnable, initialDelay, period, TimeUnit) 固定速率循环执行任务。但必须等上一个任务执行完才会开始下一个</li><li>scheduleWithFixedDelay(Runnable, initialDelay, delay, TimeUnit) 上一个任务执行完间隔delay后开始下一个</li></ul></li><li><code>newScheduledThreadPool</code>: 返回一个ScheduledExecutorService对象，线程数量可指定</li></ul><h3 id="blockingqueue" tabindex="-1"><a class="header-anchor" href="#blockingqueue" aria-hidden="true">#</a> BlockingQueue</h3><p>BlockingQueue是一种实现数据共享通道的接口，让服务线程在队列为空时进行等待，有新消息进入队列后，自动将线程唤醒。主要实现有</p><ul><li>ArrayBlockingQueue：数组实现的有界阻塞队列</li><li>LinkedBlockingQueue：链表实现的有界阻塞队列，默认MAX_VALUE个</li><li>DelayQueue：无界队列，插入永远不会阻塞，但取出时必须等待一定时延</li><li>PriorityBlockingQueue：基于优先级的无界阻塞队列（注意堆空间耗尽问题）</li><li>SynchronousQueue：没有内部容量的队列，每个put必须等待一个take，反之亦然。和容量为1的有界队列并不等价。</li></ul><p><strong>实现原理</strong> 阻塞队列内部使用Lock锁和多条件Condition阻塞控制，具体的包括一个ReentrantLock，两个Condition（notEmpty，notFull）。</p><ul><li>put/take操作都需要先获取锁，没有获取到锁的线程会自旋直到获取到锁。</li><li>接着判断队列是否可用（是否满/空），如果不可用，则会被阻塞，并释放锁。</li><li>如果队列可用，在第2点被阻塞的线程会被唤醒</li></ul><h3 id="锁接口-类" tabindex="-1"><a class="header-anchor" href="#锁接口-类" aria-hidden="true">#</a> 锁接口/类</h3><p>原理篇中的锁是Java原生的锁，一般配合synchronized使用。在JUC.locks包下提供了一些更强大，性能更好的锁接口/类。</p><p><strong>Synchronized缺点</strong></p><ul><li>如果临界区是只读操作，其实可以多线程一起执行，但使用synchronized的话，同一时间只能有一个线程执行。</li><li>synchronized无法知道线程有没有成功获取到锁</li><li>使用synchronized，如果临界区因为IO或者sleep方法等原因阻塞了，而当前线程又没有释放锁，就会导致所有线程等待。</li></ul><p><strong>锁的分类</strong></p><ul><li>重入锁：支持一个线程对资源重复加锁</li><li>非可重入锁：不支持重入</li></ul><ul><li>公平锁：先对锁获取请求的线程一定会先被满足，后对锁获取请求的线程后被满足，那这个锁就是公平的</li><li>非公平锁：不遵循公平。能提升一定效率，但可能发生线程饥饿</li></ul><ul><li>读写锁：同一时刻允许多个读线程访问</li><li>排它锁：同一时刻只允许一个线程进行访问</li></ul><p><strong>接口</strong> juc.locks包下共有三个接口：</p><ul><li>Condition：配合Lock实现加强的等待/通知模式，相比于Object的wait/notify模式，额外支持不中断等待，定时等待等需求</li><li>Lock：一些获取/释放锁的方法声明</li><li>ReadWriteLock：定义了两个方法，分别返回读锁和写锁</li></ul><p><strong>ReentrantLock</strong> 可重入锁，Lock接口的默认实现，内部有AQS-&gt;Sync-&gt;NonfairSync,FairSync两个同步器，因此支持公平/非公平两种锁，且都是独占式的排它锁。</p><p><strong>ReentrantReadWriteLock</strong> ReadWriteLock接口的默认实现，可重入，支持公平/非公平，还支持读写锁。但可能发生写饥饿。 内部同样维护了两个同步器，且维护了两个Lock的实现类ReadLock和WriteLock。</p><p><strong>StampedLock</strong> 实现了读写锁的功能，性能十分优异，可以取代ReentrantReadWriteLock。 为了解决写饥饿的问题，StampedLock借鉴CAS自旋的思想，当读时发生了写，通过重试的方式来获取新值，而不是阻塞写操作。此外，它的读锁分为乐观读锁和悲观读锁。 实现上，内部维护一个long型变量，前7位标识读锁；其余位表示写锁，且释放式递增，保留写锁痕迹，解决ABA问题，也为乐观锁提供检查变化的根据。</p><h3 id="并发容器集合" tabindex="-1"><a class="header-anchor" href="#并发容器集合" aria-hidden="true">#</a> 并发容器集合</h3><p><strong>同步容器</strong> java.util包下提供的Vector、Hashtable都是线程安全的同步容器，通过对方法加锁实现，在多线程下的复合操作需要客户端加锁保证原子性，导致性能低下。</p><p>Java5提供的并发容器在多线程编程下用于代替同步容器，针对不同的应用场景进行设计，提高容器的并发访问性，同时定义了线程安全的复合操作。</p><p><strong>并发容器</strong><img src="'+g+'" alt="并发容器" loading="lazy"></p><ol><li>并发Map - ConcurrentMap</li><li>并发Queue - ConcurrentLinkedQueue，ConcurrentLinkedDeque</li><li>并发Set - ConcurrentSkipListSet</li><li>CopyOnWrite容器 - CopyOnWriteArrayList</li></ol><h3 id="通信工具类" tabindex="-1"><a class="header-anchor" href="#通信工具类" aria-hidden="true">#</a> 通信工具类</h3><ul><li>Semaphore：限制线程数量。内部维护继承AQS的Sync，想要的资源数小于现有资源时进入AQS等待队列</li><li>Exchanger：两个线程交换数据，支持泛型。两个线程都执行exchange才会继续运行。底层使用CAS和park/unpark实现等待状态的切换</li><li>CountDownLatch：线程等待直到计数器减为0时开始工作。内部使用AQS实现</li><li>CyclicBarrier：类似CountDownLatch，但可以重置计数器以复用。内部使用Lock+Condition实现的等待/通知模式</li><li>Phaser：增强的CyclicBarrier，可以动态调整计数器值。内部使用两个基于Fork/Join框架的AtomicReference实现。</li></ul><h3 id="fork-join框架" tabindex="-1"><a class="header-anchor" href="#fork-join框架" aria-hidden="true">#</a> Fork/Join框架</h3><p>Fork/Join框架是一个实现了ExecutorService接口的多线程处理器，它专为那些可以通过递归分解成更细小的任务而设计，最大化的利用多核处理器来提高应用程序的性能。 <img src="'+k+'" alt="fork_join流程图" loading="lazy"></p><p>Fork/Join框架会将任务分配给线程池中的线程，执行任务时使用了工作窃取算法。 <img src="'+m+'" alt="工作窃取算法运行流程图" loading="lazy"></p><p><strong>具体实现</strong></p><ul><li>ForkJoinTask是一个类似普通线程的实体，但更加轻量。 <ul><li>fork()方法使用线程池中的空闲线程异步提交任务</li><li>join()方法等待处理任务的线程执行完毕，获得返回值</li><li>一般创建任务继承两个子类RecursiveAction、RecursiveTask</li></ul></li><li>ForkJoinPool是用于执行ForkJoinTask任务的执行（线程）池 <ul><li>内部维护WorkQueue，一个双端队列结构的工作队列</li><li>当工作线程在处理自己的工作队列时，会从队列首取任务来执行</li><li>如果是窃取其他队列的任务时，窃取的任务位于所属任务队列的队尾</li><li>任务默认提交到common公用线程池</li></ul></li></ul><p><img src="'+b+'" alt="join流程图" loading="lazy"></p><h3 id="stream并行计算" tabindex="-1"><a class="header-anchor" href="#stream并行计算" aria-hidden="true">#</a> Stream并行计算</h3><p>Java8开始的Stream接口默认是使用串行的方式，parallel()使计算并行化。</p><p>底层原理是使用了Fork/Join框架，源码根据是否是parallel，创建流对应的任务，例如ReduceTask，继承关系：</p><blockquote><p>ReduceTask -&gt; AbstractTask -&gt; CountedCompleter -&gt; ForkJoinTask</p></blockquote><p>间接调用了ForkJoinTask::invoke()。一个Java进程的Stream并行计算任务共享一个线程池。</p><h3 id="计划任务" tabindex="-1"><a class="header-anchor" href="#计划任务" aria-hidden="true">#</a> 计划任务</h3><p>最初Java的定时任务由Timer类完成，但：</p><ul><li>Timer是单线程模式；</li><li>如果在执行任务期间某个TimerTask耗时较久，那么就会影响其它任务的调度；</li><li>Timer的任务调度是基于绝对时间的，对系统时间敏感；</li><li>Timer不会捕获执行TimerTask时所抛出的异常，由于Timer是单线程，所以一旦出现异常，则线程就会终止，其他任务也得不到执行。</li></ul><p>JDK 1.5提供了ScheduledThreadPoolExecutor类用于计划任务，可以实现</p><ul><li>在给定的延迟之后运行任务</li><li>周期性重复执行任务</li></ul><p>ScheduledThreadPoolExecutor的runAndReset()方法在任务结束后不更新状态，也不设置执行结果，从而实现任务的多次执行。最后调用delayedExecute()将任务添加到等待队列，最后让线程池的工作线程getTask()获取任务并执行。</p><p>内部使用DelayedWorkQueue保存等待的任务。底层是堆结构实现的优先队列，队列的队首leader保存最近将要执行的任务，使用ReentrantLock和Condition保证leader竞争的一致性。</p><h3 id="threadlocal" tabindex="-1"><a class="header-anchor" href="#threadlocal" aria-hidden="true">#</a> ThreadLocal</h3><p><strong>数据一致性问题</strong> 发生在多个主体对同一份数据无法达成共识。这里的多个主体，可能是多线程，也可能是多个服务器节点。</p><p>解决方法有三个：</p><ul><li>排队：如OS中的锁、互斥量、管程、屏障等</li><li>投票：如分布式系统中的Raft、Paxos</li><li>避免：如Git、ThreadLocal</li></ul><p><strong>线程模型</strong> ThreadLocal提供了线程局部变量，一个线程局部变量在多个线程中，分别有独立的值（副本）。</p><p><img src="'+f+'" alt="ThreadLocal模型" loading="lazy"></p><p><strong>实现原理</strong> 内部实现包括ThreadLocalMap, Entry...Map被Thread对象私有，里面包含多个Entry，Entry的Key就是一个ThreadLocal类型引用。</p><p><img src="'+y+'" alt="ThreadLocal概念图2" loading="lazy"></p><p>弱引用(WeakReferences): 如果某个对象仅剩下弱引用指向它，那么下一次GC的时候该对象就会被回收掉</p><p>如果使用InheritableThreadLocal，当新建一个子线程时，会将当前线程的 inheritableThreadLocals里的 ThreadLocalMap 赋值给子线程的 inheritableThreadLocals 变量，从而继承父线程的私有数据。但无法向线程池中的子线程传递数据，因为传递数据仅发生在线程初始化的时候。</p><p><strong>使用场景</strong>：资源持有、线程一致、线程安全、并发计算</p>',57);function _(C,j){const n=r("RouterLink"),t=r("Mermaid");return d(),c("div",null,[v,e("blockquote",null,[e("p",null,[a("关于 Java 线程："),i(n,{to:"/coding/Thread.html"},{default:o(()=>[a("Thread 源码阅读")]),_:1})])]),T,J,x,e("p",null,[a("见 "),i(n,{to:"/coding/Synchronized.html"},{default:o(()=>[a("Synchronized 锁机制")]),_:1})]),M,e("p",null,[a("见 "),i(n,{to:"/coding/LockSupport.html"},{default:o(()=>[a("LockSupport")]),_:1})]),L,i(t,{id:"mermaid-713",code:"eJxLL0osyFDwCeJSUHiye/fTroXP+ic82bVEQVdX4emG/ie7+pLzi1ID8vNzgjOrUoGidgqpeSmGGk872l629j7ftf/5iu5nnctfLOzRxGbCkuXYTIAoeNqx4fnazqf7Wl/OmP+0YzpI/bOOCU+75oPUQGQgJgMNxqnj6ZKNL7YsRTIV6qSNC8Cy2ze92LcPqO3ZnAagW6ByUzdA1HdPer57LpoVKNqfzVmFWzswGIywBgMA/+y9Cg=="}),A])}const P=s(S,[["render",_],["__file","JUC.html.vue"]]);export{P as default};
